{
  "name": "claude-voyager",
  "version": "0.1.0",
  "description": "Chrome 扩展：为 claude.ai 网页端增加右侧时间轴导航、对话整理与按钮中文化",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --config vite.content.config.ts --watch --mode development & vite build --config vite.config.ts --watch --mode development",
    "build": "vite build --config vite.content.config.ts && vite build --config vite.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
